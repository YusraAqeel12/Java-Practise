function check(){
    const index = string.indexOf(word) //word mai same sub dhoondo
    if ( index != -1){
        return string.slice(0,index)+string.slice(index+word.length) //add not ,
        //index kai baad waalay ko add karta hai with bacha howa length
        
    }

}

